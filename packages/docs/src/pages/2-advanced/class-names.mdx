import { CssPropExample } from '../../examples/class-names';

# Class Names

Sometimes we want to pass consumers some styles without forcing them into taking an extra `div` or `span`,
or if we use a third party component that takes something other than a `className`.

<CssPropExample />
