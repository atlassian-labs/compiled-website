---
section: 90-Advanced
order: 1
---

# Class Names

The `ClassNames` component is used when wanting to more control over a component which has associated styles,
but not necessarily tied to an element.

```
example here
```

## Dynamic declarations

Dynamic declarations are powered by [CSS variables](https://developer.mozilla.org/en-US/docs/Web/CSS/Using_CSS_custom_properties),
make sure to set the `style` prop.

```
example here
```

## Composing styles

Composing styles through props allows your consumers to add and override extra styles,
make sure to pass through `className` and `style`.

```
example here
```
