---
order: 4
section: 99-Packages
---

# @compiled/jest-css-in-js

```bash
npm i @compiled/jest-css-in-js
```

## Jest matcher

Used to enable the compiled CSS matcher in Jest.

```jsx
import { toHaveCompiledCss } from '@compiled/jest-css-in-js';

expect.extend({
  toHaveCompiledCss,
});
```
