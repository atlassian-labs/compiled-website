import {
  CompositionObject,
  CompositionOrder,
  CompositionImports,
} from '../../examples/composition';

# Composition

Composition with Compiled is mostly the same as runtime based libraries,
you define your styles and then you can compose them together.

<CompositionObject />

More text

<CompositionOrder />

More text

<CompositionImports />
